# GOB .claude Notes

This folder is project-specific. The source of truth for shared Claude
configuration lives in the `claude-config` repo.

Keep only GOB-specific files here and avoid copying caches or global
settings from `claude-config`.
